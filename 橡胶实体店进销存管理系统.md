# 橡胶实体店进销存管理系统

（最终定稿版 V12.0）

版本：V12.0 (最终定稿) | 编写日期：2025-03-26

> 本版本已完成所有修改：销售出库记日志、增加采购明细修改接口、去掉 received 状态让确认收货直达 completed、所有字段加_zj 后缀。可直接用于毕业设计开发与答辩。

---

## 一、项目名称

**橡胶实体店进销存管理系统**  
基于 Spring Boot + Vue3 + UniApp 的轻量化进销存与赊账管理平台

## 二、技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | Spring Boot 3.x (JDK 17) |
| 数据库 | MySQL 8.0 |
| ORM | MyBatis-Plus |
| 安全与认证 | Spring Security + JWT |
| API 测试 | Apifox（所有接口通过 Apifox 调试、Mock 及文档共享） |
| 管理后台前端 | Vue 3 + Vite + Element Plus |
| 移动端 | UniApp (Vue3 语法，支持 H5/App) |
| 文件存储 | 本地磁盘存储（静态资源映射） |
| 数据可视化 | ECharts |
| 数据库版本控制 | 预置 SQL 脚本（手动执行） |

## 三、开发工具

| 工具 | 用途 |
|------|------|
| IntelliJ IDEA 2023+ | 后端开发 |
| VS Code 1.8+ | 管理后台前端开发 |
| HBuilderX 3.9+ | UniApp 移动端开发 |
| Navicat 16+ | 数据库管理 |
| Apifox | 接口调试、文档、Mock |
| Git (Gitee) | 版本控制 |

## 四、角色与账号体系

### 4.1 角色定义

- **customer（顾客）**：仅移动端，浏览商品、下单、定制、还款、个人中心。
- **merchant（商家）**：拥有 Web 管理后台 + 移动端助手，可管理商品、订单、定制、采购、库存、用户、系统配置等。**商家移动端新增商品浏览与搜索功能**，方便移动场景下查询库存和价格。
- **factory（厂家）**：仅移动端，只接收和操作与自己关联的采购单（确认接单、发货）。

### 4.2 管理员账号创建

系统部署时预置默认商家账号：  
- 手机号：`13800000000`  
- 密码：`admin123`（BCrypt 加密）  
- 角色：`merchant`  

商家可在后台的用户管理中添加其他商家或修改角色。本项目不提供商家注册接口。所有用户均可通过接口 `PUT /api/auth/password` 修改密码。

### 4.3 顾客余额与赊账机制

- 新注册顾客默认余额 `0.00` 元，商家可在后台为该用户设置赊账额度（`credit_limit`）。
- 顾客可通过**极简充值接口**自行增加余额（模拟即时到账，无第三方）。
- 下单时可选择「余额支付」或「赊账支付」，余额不足或赊账超额度时拒绝。
- 所有余额变动记录在 `balance_log_zj` 表，类型包括：充值、消费、还款、退款、管理员调整（`admin_adjust`）。
- 赊账产生的应收账款记录在 `receivable_zj` 表，顾客可主动用余额还款，商家也可登记线下收款。

### 4.4 收货地址（极简）

- 每个顾客保存一个默认收货地址，存储于 `user_zj.default_receiver_*_zj` 系列字段。
- 下单时快照地址至订单 `order_zj.receiver_*_zj`，用户后续修改地址不影响已有订单。
- 提交订单前强制校验地址（省市区+详细地址）不为空。

## 五、数据库设计（共 15 张表）

> 设计原则：所有业务表名统一添加项目后缀 `_zj`，**所有字段名也统一添加后缀 `_zj`**（例如 `phone_zj`、`real_name_zj`），确保命名体系完全一致。主键统一使用 `id_zj`，外键对应 `表名_id_zj`。

### 5.1 表清单

| 表名 | 说明 |
|------|------|
| user_zj | 顾客/商家/厂家 |
| category_zj | 商品分类 |
| product_zj | 商品（橡胶制品） |
| order_zj | 订单主表 |
| order_item_zj | 订单明细 |
| order_status_log_zj | 订单状态变更日志 |
| customization_zj | 定制主表 |
| customization_item_zj | 定制报价明细 |
| purchase_zj | 采购单 |
| purchase_item_zj | 采购明细 |
| receivable_zj | 应收账款（赊账） |
| receipt_zj | 收款记录 |
| stock_log_zj | 库存出入库流水 |
| balance_log_zj | 余额变动流水 |
| sys_config_zj | 系统配置（KV） |

### 5.2 表结构详情（所有字段均已加后缀 _zj）

#### 表 1：user_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键自增 |
| phone_zj | varchar(20) UNIQUE | 手机号（登录账号） |
| password_zj | varchar(128) | BCrypt 加密密码 |
| role_zj | enum('customer','merchant','factory') | 角色 |
| real_name_zj | varchar(50) | 真实姓名 |
| avatar_zj | varchar(255) | 头像 URL |
| balance_zj | decimal(10,2) | 账户余额，默认 0.00 |
| points_zj | int(11) | 积分（预留） |
| credit_limit_zj | decimal(10,2) | 赊账额度，默认 0.00 |
| company_name_zj | varchar(100) | 厂家公司名（仅厂家填写） |
| default_receiver_name_zj | varchar(50) | 默认收货人 |
| default_receiver_phone_zj | varchar(20) | 默认收货电话 |
| default_province_zj | varchar(20) | 省 |
| default_city_zj | varchar(20) | 市 |
| default_district_zj | varchar(20) | 区 |
| default_detail_address_zj | varchar(200) | 详细地址 |
| status_zj | tinyint(1) | 1-正常，0-禁用 |
| created_at_zj | datetime | 注册时间 |
| updated_at_zj | datetime | 更新时间 |

#### 表 2：category_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| name_zj | varchar(50) | 分类名称 |
| parent_id_zj | int(11) | 父级ID，0为顶级 |
| sort_zj | int(11) | 排序 |
| icon_zj | varchar(255) | 图标 URL |
| created_at_zj | datetime | 创建时间 |

#### 表 3：product_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| category_id_zj | bigint(20) | 分类ID |
| name_zj | varchar(100) | 商品名称 |
| description_zj | text | 描述 |
| images_zj | json | 图片数组 `["url1","url2"]` |
| price_zj | decimal(10,2) | 销售价 |
| stock_zj | int(11) | 库存数量 |
| warning_stock_zj | int(11) DEFAULT 10 | 库存预警阈值 |
| status_zj | enum('on','off') | 上架/下架 |
| factory_id_zj | bigint(20) | 关联厂家ID（可为空） |
| is_customized_zj | tinyint(1) DEFAULT 0 | 是否来自定制（1-是，0-否） |
| created_at_zj | datetime | 创建时间 |
| updated_at_zj | datetime | 修改时间 |

#### 表 4：order_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| order_no_zj | varchar(32) UNIQUE | 订单号（自动生成） |
| user_id_zj | bigint(20) | 顾客ID |
| total_amount_zj | decimal(10,2) | 商品总额 |
| actual_amount_zj | decimal(10,2) | 实付金额（等于总额） |
| payment_method_zj | enum('balance','credit') | 支付方式 |
| status_zj | enum('paid','accepted','shipped','completed','cancelled','refunding','refunded') | 订单状态（已移除 pending 和 received，确认收货直达 completed） |
| receiver_name_zj | varchar(50) | 收货人快照 |
| receiver_phone_zj | varchar(20) | 收货电话快照 |
| province_zj | varchar(20) | 省 |
| city_zj | varchar(20) | 市 |
| district_zj | varchar(20) | 区 |
| detail_address_zj | varchar(200) | 详细地址 |
| express_company_zj | varchar(50) | 快递公司 |
| tracking_no_zj | varchar(50) | 快递单号 |
| mark_zj | varchar(200) | 备注 |
| paid_at_zj | datetime | 支付时间 |
| shipped_at_zj | datetime | 发货时间 |
| finished_at_zj | datetime | 完成时间 |
| created_at_zj | datetime | 下单时间 |
| updated_at_zj | datetime | 更新时间 |

#### 表 5：order_item_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| order_id_zj | bigint(20) | 订单ID |
| product_id_zj | bigint(20) | 商品ID |
| product_name_zj | varchar(100) | 商品名称快照 |
| product_image_zj | varchar(255) | 商品主图快照 |
| price_zj | decimal(10,2) | 下单时单价 |
| quantity_zj | int(11) | 数量 |
| subtotal_zj | decimal(10,2) | 小计 |

#### 表 6：order_status_log_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| order_id_zj | bigint(20) | 订单ID |
| from_status_zj | varchar(20) | 原状态 |
| to_status_zj | varchar(20) | 新状态 |
| operator_id_zj | bigint(20) | 操作人ID |
| remark_zj | text | 备注 |
| created_at_zj | datetime | 操作时间 |

#### 表 7：customization_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| user_id_zj | bigint(20) | 顾客ID |
| status_zj | enum('pending_quote','quoted','confirmed','converted','cancelled') | 定制状态 |
| description_zj | text | 需求描述 |
| reference_images_zj | json | 参考图片数组 |
| total_quoted_price_zj | decimal(10,2) | 商家总报价 |
| order_id_zj | bigint(20) | 确认后生成的订单ID（可空） |
| created_at_zj | datetime | 创建时间 |
| updated_at_zj | datetime | 更新时间 |

#### 表 8：customization_item_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| customization_id_zj | bigint(20) | 定制ID |
| product_spec_zj | varchar(200) | 规格描述 |
| quantity_zj | int(11) | 数量 |
| unit_price_zj | decimal(10,2) | 商家报价单价 |

#### 表 9：purchase_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| order_no_zj | varchar(32) UNIQUE | 采购单号 |
| factory_id_zj | bigint(20) | 厂家ID |
| total_amount_zj | decimal(10,2) | 采购总额 |
| status_zj | enum('pending','confirmed','shipped','received','cancelled') | 采购状态 |
| expected_delivery_date_zj | date | 预计交货日期 |
| express_company_zj | varchar(50) | 物流公司（厂家填写） |
| tracking_no_zj | varchar(50) | 物流单号（厂家填写） |
| created_at_zj | datetime | 创建时间 |
| updated_at_zj | datetime | 更新时间 |

#### 表 10：purchase_item_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| purchase_id_zj | bigint(20) | 采购单ID |
| product_id_zj | bigint(20) | 关联商品（可为空） |
| product_name_zj | varchar(100) | 商品名称 |
| spec_zj | varchar(200) | 规格 |
| quantity_zj | int(11) | 数量 |
| unit_price_zj | decimal(10,2) | 单价 |
| subtotal_zj | decimal(10,2) | 小计 |

#### 表 11：receivable_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| order_id_zj | bigint(20) | 订单ID |
| user_id_zj | bigint(20) | 欠款顾客ID |
| amount_owed_zj | decimal(10,2) | 应收总额 |
| amount_paid_zj | decimal(10,2) | 已还金额，默认 0 |
| status_zj | enum('unpaid','partially_paid','paid','void') | 状态 |
| due_date_zj | date | 到期日（可空） |
| created_at_zj | datetime | 创建时间 |
| updated_at_zj | datetime | 更新时间 |

#### 表 12：receipt_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| receivable_id_zj | bigint(20) | 应收款ID |
| amount_zj | decimal(10,2) | 收款金额 |
| payment_method_zj | enum('balance','cash','bank_transfer') | 收款方式 |
| operator_id_zj | bigint(20) | 操作人ID（顾客或商家） |
| remark_zj | varchar(200) | 备注 |
| created_at_zj | datetime | 收款时间 |

#### 表 13：stock_log_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| product_id_zj | bigint(20) | 商品ID |
| change_quantity_zj | int(11) | 变动数量（正为入，负为出） |
| current_stock_zj | int(11) | 变动后库存快照 |
| type_zj | enum('purchase_in','sale_out','manual_in','manual_out','refund_in') | 变动类型 |
| reference_id_zj | bigint(20) | 关联采购单/订单ID |
| remark_zj | varchar(200) | 备注 |
| created_at_zj | datetime | 操作时间 |

#### 表 14：balance_log_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| user_id_zj | bigint(20) | 用户ID |
| change_amount_zj | decimal(10,2) | 变动金额（正为增，负为减） |
| current_balance_zj | decimal(10,2) | 变动后余额 |
| type_zj | enum('recharge','consume','repay','refund','withdraw','admin_adjust') | 变动类型 |
| reference_id_zj | bigint(20) | 关联订单/收款ID |
| remark_zj | varchar(200) | 备注 |
| created_at_zj | datetime | 操作时间 |

#### 表 15：sys_config_zj

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id_zj | bigint(20) PK | 主键 |
| config_key_zj | varchar(50) | 唯一键 |
| config_value_zj | varchar(255) | 配置值 |
| remark_zj | varchar(200) | 说明 |
| updated_at_zj | datetime | 更新时间 |

## 六、后端核心功能与业务规则

### 6.1 用户与账户

- 注册/登录：手机号+密码，注册后默认角色 `customer`，余额 0，赊账额度 0（商家后台可修改）。
- 修改密码：所有登录用户可通过接口 `PUT /api/auth/password` 修改密码，需提供旧密码。
- 地址管理：顾客可获取/更新默认收货地址（省市区+详细），下单时快照到订单。
- 充值：顾客调用 `POST /api/user/recharge` 传入金额，直接加余额，记录 `balance_log`（type=recharge）。
- 商家调额：管理员在后台修改顾客余额/赊账额度，记录日志 type=`admin_adjust`。

### 6.2 商品管理

- 分类：无限级分类（父子关系），商家在 Web 端维护。
- 商品：支持图片 JSON 存储、价格、库存、预警、关联厂家。
- 库存：所有库存变动（包括销售出库、采购入库、退款入库、手动调整）均写入 `stock_log_zj` 表，确保流水完整可追溯。**订单支付成功时，除执行乐观锁扣减库存外，还需插入一条 `type=sale_out` 的库存流水，`reference_id` 关联订单ID。**

### 6.3 订单与支付

**下单**：顾客移动端提交商品列表、支付方式、收货地址（可选，默认取用户地址）。  
- 校验：商品存在、上架、库存充足；若使用余额支付则 `balance >= total_amount`；若使用赊账，则计算当前可用额度 = `credit_limit - (unpaid应收总额 - 已还部分)`，必须覆盖订单额。  
- 支付：立即扣减余额或生成应收记录，库存扣减（乐观锁），记录销售出库流水，订单状态变为 `paid`。  
- 订单状态流转：`paid` → 商家接单 `accepted` → 发货 `shipped` → **顾客确认收货直接变 `completed`**。  
- 取消/退款：仅支持已支付未发货状态 (`paid` 或 `accepted`)。余额支付退款退回余额，记录流水；赊账支付时作废应收（若已部分还款则退还已还部分至余额）。库存回加，记录 `refund_in` 类型库存流水。状态变为 `refunded`。**顾客只能取消自己的订单，商家可取消任何订单。**  
- 所有状态变更均记录 `order_status_log_zj`。

### 6.4 定制业务

- 顾客提交定制需求（描述+图片，依赖文件上传接口），商家在 Web 端逐项报价（规格、数量、单价）。
- 顾客确认报价（接口需传支付方式）→ 系统自动生成临时商品（`is_customized=1`，`status='off'`），并创建订单（状态 `paid`），定制记录关联 `order_id`，状态变为 `confirmed`。
- 商家可选调用“转常规商品”接口，使定制商品进入正规商品库。

### 6.5 采购与入库

- 商家创建采购单，指定厂家、明细（可关联现有商品，也可暂不关联后续通过修改接口补全）。
- **采购单修改**：商家可通过 `PUT /api/purchases/{id}` 修改采购单基本信息及明细（例如补全 `product_id`），**仅限状态为 `pending` 的采购单**；修改后系统自动重新计算总额。
- 厂家移动端可查看自己相关的采购单，操作：确认接单（`pending→confirmed`）、发货（`confirmed→shipped`，可填物流）。
- 商家确认收货（`shipped→received`）时，遍历采购明细，对每个已关联 `product_id` 的明细更新对应商品库存，记录 `stock_log` (type=purchase_in)。若明细未关联商品，则提示错误，阻止收货。

### 6.6 赊账与还款

- 赊账支付时生成 `receivable` 记录，状态 `unpaid`。
- 顾客移动端可主动还款（从余额扣款），`payment_method` **仅允许为 `balance`**；商家后台可登记线下还款（现金、转账），`payment_method` 可选 `cash` 或 `bank_transfer`。
- 还款时更新 `receivable` 的 `amount_paid`，状态转为 `partially_paid` 或 `paid`。

### 6.7 库存管理

- 预警：商品库存低于 `warning_stock` 时，可通过接口 `GET /api/stock/warnings` 获取预警列表。
- 手动调整：商家可通过 `/api/stock/adjust` 直接增减库存，记录 `manual_in/out` 流水。

## 七、API 接口文档

**基础路径**：`http://localhost:8080/api`  
**鉴权**：Header `Authorization: Bearer <token>`  
**角色标识**：🔓公开  🔒所有登录用户  ⚙️商家专属  📦厂家专属

### 7.1 认证与用户

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| POST | /api/auth/login | 登录，返回JWT | 🔓 |
| PUT | /api/auth/password | 修改密码（需旧密码） | 🔒 |
| GET | /api/user/profile | 获取当前用户信息 | 🔒 |
| PUT | /api/user/profile | 更新个人资料与默认地址 | 🔒 |
| POST | /api/user/recharge | 极简充值（顾客） | 🔒 |
| GET | /api/user/balance_logs | 余额流水（分页） | 🔒 |

### 7.2 商品模块

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| GET | /api/products | 商品列表（支持搜索，商家返回全部商品，顾客仅返回已上架商品） | 🔓/🔒 |
| GET | /api/products/{id} | 商品详情 | 🔓 |
| POST | /api/products | 新增商品（商家） | ⚙️ |
| PUT | /api/products/{id} | 修改商品 | ⚙️ |
| DELETE | /api/products/{id} | 下架/删除 | ⚙️ |
| POST | /api/upload | 上传图片返回URL | 🔒 |

### 7.3 分类模块

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| GET | /api/categories | 分类树 | 🔓 |
| POST | /api/categories | 新增分类 | ⚙️ |
| PUT | /api/categories/{id} | 修改分类 | ⚙️ |
| DELETE | /api/categories/{id} | 删除分类 | ⚙️ |

### 7.4 订单模块

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| POST | /api/orders | 下单（顾客） | 🔒 |
| GET | /api/orders | 我的订单/全部订单 | 🔒⚙️ |
| GET | /api/orders/{id} | 订单详情（含明细、日志） | 🔒⚙️ |
| PUT | /api/orders/{id}/status | 商家更新状态（接单、发货等） | ⚙️ |
| PUT | /api/orders/{id}/ship | 填写物流信息（商家） | ⚙️ |
| PUT | /api/orders/{id}/receive | 确认收货（顾客/商家），订单状态变为 completed | 🔒⚙️ |
| POST | /api/orders/{id}/cancel | 取消订单（顾客仅限本人未发货订单，商家可取消任何） | 🔒⚙️ |

### 7.5 定制模块

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| POST | /api/customizations | 顾客提交定制（可上传图片） | 🔒 |
| GET | /api/customizations | 我的/全部定制列表 | 🔒⚙️ |
| GET | /api/customizations/{id} | 定制详情 | 🔒⚙️ |
| PUT | /api/customizations/{id}/quote | 商家报价 | ⚙️ |
| PUT | /api/customizations/{id}/confirm | 顾客确认（自动生成订单） | 🔒 |
| POST | /api/customizations/{id}/convert-to-product | 转为常规商品 | ⚙️ |

### 7.6 采购模块

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| POST | /api/purchases | 商家创建采购单 | ⚙️ |
| GET | /api/purchases | 商家全部 / 厂家仅自己的 | ⚙️📦 |
| GET | /api/purchases/{id} | 采购单详情 | ⚙️📦 |
| PUT | /api/purchases/{id} | **修改采购单（含明细）**，仅限 pending 状态 | ⚙️ |
| PUT | /api/purchases/{id}/status | 厂家/商家操作状态 | ⚙️📦 |
| PUT | /api/purchases/{id}/logistics | 厂家填写物流信息 | 📦 |

### 7.7 应收/收款

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| GET | /api/receivables | 顾客自己的/商家全部 | 🔒⚙️ |
| GET | /api/receivables/{id} | 详情（含收款记录） | 🔒⚙️ |
| POST | /api/receivables/{id}/receipts | 还款/登记收款（顾客仅限 balance，商家可 cash/bank） | 🔒⚙️ |

### 7.8 库存管理

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| GET | /api/stock/logs | 库存流水（分页） | ⚙️ |
| POST | /api/stock/adjust | 手动调整库存 | ⚙️ |
| GET | /api/stock/warnings | 库存预警列表（低于阈值的商品） | ⚙️ |

### 7.9 用户管理（商家）

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| GET | /api/admin/users | 用户列表 | ⚙️ |
| GET | /api/admin/users/{id} | 用户详情 | ⚙️ |
| PUT | /api/admin/users/{id} | 修改余额/额度/状态 | ⚙️ |

### 7.10 系统配置

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| GET | /api/configs | 获取所有配置 | 🔓 |
| PUT | /api/configs/{key} | 修改配置项 | ⚙️ |

### 7.11 数据统计

| 方法 | 路径 | 说明 | 角色 |
|------|------|------|------|
| GET | /api/admin/statistics/dashboard | 仪表盘数据（销售额趋势、订单量、库存预警等） | ⚙️ |

## 八、Vue 管理后台

| 路由 | 页面组件 | 说明 |
|------|----------|------|
| /login | LoginView | 商家登录 |
| /dashboard | DashboardView | 仪表盘（调用 7.11 接口） |
| /product/list | ProductList | 商品列表（可查看全部商品） |
| /product/form/:id? | ProductForm | 新增/编辑商品 |
| /category/list | CategoryList | 分类树管理 |
| /order/list | OrderList | 全部订单（状态 Tab、搜索、操作） |
| /order/detail/:id | OrderDetail | 订单详情 |
| /customization/list | CustomizationList | 定制请求管理 |
| /customization/:id | CustomizationDetail | 报价、转商品等操作 |
| /purchase/list | PurchaseList | 采购单列表 |
| /purchase/form/:id? | PurchaseForm | 新增/编辑采购单（支持修改明细） |
| /stock/log | StockLog | 出入库日志 |
| /stock/manual | StockManual | 手动出入库 |
| /user/list | UserList | 用户管理 |
| /system/config | SystemConfig | 系统配置编辑页 |

## 九、UniApp 移动端

### 9.1 底部导航设计

#### 顾客端（customer）

| Tab | 页面路径 | 功能 |
|-----|----------|------|
| 首页 | /pages/index/index | 商品分类浏览、搜索 |
| 定制 | /pages/custom/index | 定制列表、新建定制 |
| 订单 | /pages/order/list | 我的订单（按状态 Tab） |
| 我的 | /pages/mine/index | 个人中心（余额、应收款、地址、修改密码） |

#### 商家移动端（merchant）

| Tab | 页面路径 | 功能 |
|-----|----------|------|
| 商品 | /pages/product/list | 商品浏览、搜索、详情 |
| 订单 | /pages/order/list | 全部订单（操作接单/发货） |
| 我的 | /pages/mine/index | 扫码入库（可扩展）、设置、修改密码 |

#### 厂家移动端（factory）

| Tab | 页面路径 | 功能 |
|-----|----------|------|
| 采购单 | /pages/purchase/list | 自己的采购单列表 |
| 我的 | /pages/mine/index | 设置、修改密码 |

### 9.2 关键页面功能

| 页面 | 功能描述 |
|------|----------|
| 商品详情 | 所有角色可查看，展示图片、价格、库存（商家端可见全部商品，顾客端仅上架） |
| 下单页 | 选择支付方式，确认地址，提交后实时扣款 |
| 确认收货 | 顾客点击“确认收货”按钮，订单直接完成 |
| 还款 | 顾客主动还款，支付方式固定为 balance |
| 修改密码 | 在我的页面提供入口，调用 /api/auth/password |
| 定制提交 | 支持上传参考图片（调用 /api/upload） |

## 十、项目核心难点与解决策略

| 难点 | 详细分析与解决策略 |
|------|---------------------|
| **1. 多角色权限与端隔离** | 通过 Spring Security + JWT 实现角色级别接口保护，前端根据角色动态渲染不同导航。 |
| **2. 赊账额度动态计算** | 可用额度 = 设定额度 - 当前所有未还清应收总额。每次下单实时计算，避免超赊。 |
| **3. 定制转订单闭环** | 商家报价 → 顾客确认 → 自动生成临时商品和订单，保证数据一致性，事务包裹。 |
| **4. 库存并发扣减与流水完整性** | 采用乐观锁防止超卖，同时每笔销售出库均记录 `stock_log_zj`（type=sale_out），保证库存流水可追溯，账实相符。 |
| **5. 退款业务一致性** | 退款时根据支付方式不同，分别处理余额退回或应收作废，并回加库存、记录流水，所有操作在同一事务。 |
| **6. 采购入库灵活性** | 采购明细支持先创建后补全信息：商家可在 `pending` 状态通过 `PUT /api/purchases/{id}` 修改明细，强制收货时明细必须关联有效商品，否则阻塞。 |
| **7. 地址快照设计** | 用户修改地址不影响历史订单，下单时将当前默认地址复制进订单表。 |
| **8. 商家移动端功能边界** | 移动端仅提供商品浏览（含全部状态）、订单处理等高频操作，不开放编辑权限，避免误操作。 |
| **9. 全字段后缀防冲突** | **所有业务字段名统一附加 `_zj` 后缀**，与表名后缀呼应，杜绝命名冲突与理解歧义。 |
| **10. 接口与前端对齐** | 本次版本补全了修改密码、仪表盘统计、库存预警、采购修改、确认收货、顾客上传等接口，保证前后端功能一一对应。 |

## 十一、项目开发与答辩流程

- **阶段一：环境搭建与初始化（1-2 天）**
- **阶段二：后端核心接口开发（3-4 天）**
- **阶段三：前端与移动端开发（3-4 天）**
- **阶段四：联调与测试（2 天）**
- **阶段五：答辩准备（1 天）**

## 十二、附录：预置 SQL 关键代码示例

```sql
-- 1. 默认商家账号（密码 admin123，BCrypt 加密后）
INSERT INTO user_zj (phone_zj, password_zj, role_zj, real_name_zj, balance_zj, credit_limit_zj, status_zj, created_at_zj)
VALUES ('13800000000', '$2a$10$...', 'merchant', '系统管理员', 0.00, 0.00, 1, NOW());

-- 2. 示例厂家账号
INSERT INTO user_zj (phone_zj, password_zj, role_zj, company_name_zj, status_zj, created_at_zj)
VALUES ('13900000001', '$2a$10$...', 'factory', 'xx橡胶厂', 1, NOW());

-- 3. 商品分类示例
INSERT INTO category_zj (name_zj, parent_id_zj, sort_zj, created_at_zj) VALUES 
('橡胶原料', 0, 1, NOW()),
('橡胶制品', 0, 2, NOW()),
('O型圈', 2, 1, NOW()),
('密封条', 2, 2, NOW());

-- 4. 商品示例
INSERT INTO product_zj (category_id_zj, name_zj, description_zj, images_zj, price_zj, stock_zj, warning_stock_zj, status_zj, created_at_zj)
VALUES (1, '天然橡胶片', '优质天然橡胶', '["/images/rubber1.jpg"]', 25.00, 500, 50, 'on', NOW());

-- 5. 系统配置
INSERT INTO sys_config_zj (config_key_zj, config_value_zj, remark_zj) VALUES 
('site_name', '橡胶进销存', '系统名称'),
('auto_complete_days', '7', '自动完成天数');

-- 6. 推荐索引
ALTER TABLE order_zj ADD INDEX idx_order_user (user_id_zj);
ALTER TABLE order_item_zj ADD INDEX idx_item_order (order_id_zj);
ALTER TABLE stock_log_zj ADD INDEX idx_stock_product (product_id_zj);
ALTER TABLE balance_log_zj ADD INDEX idx_balance_user (user_id_zj);
ALTER TABLE receivable_zj ADD INDEX idx_receivable_user (user_id_zj);
ALTER TABLE purchase_zj ADD INDEX idx_purchase_factory (factory_id_zj);