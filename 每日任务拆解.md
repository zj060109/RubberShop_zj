# 橡胶实体店进销存管理系统开发任务逐日拆解（基于V12.0设计文档）

> 本拆解依据《橡胶实体店进销存管理系统（最终定稿版 V12.0）》编写，将全部功能分解为15天（每天6小时）的详细任务。开发者可按实际情况调整天数，重点在于清晰掌握每天的目标、工作内容和检验标准。

> 技术栈：Spring Boot 3 + MyBatis-Plus + Spring Security + JWT + MySQL + Vue3 + Element Plus + UniApp
> API测试：Apifox  数据库字段统一附加 `_zj` 后缀

---

## 第1天：后端项目初始化与数据库基础

**目标**：创建Spring Boot项目，搭建数据库，生成基础代码结构，插入预置数据。

**前置条件**：已安装JDK17、MySQL8、IDEA等开发工具，手动创建数据库`rubber_shop`。

### 任务详细 (6h)

#### 一、创建Maven项目（1h）

通过Spring Initializr或手动创建Maven项目，添加以下依赖：web、mysql、mybatis-plus、validation、spring-security、jjwt、lombok。

**pom.xml 核心配置**：

```xml
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
</parent>

<properties>
    <java.version>17</java.version>
    <mybatis-plus.version>3.5.7</mybatis-plus.version>
    <jjwt.version>0.12.3</jjwt.version>
</properties>

<dependencies>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        <version>${mybatis-plus.version}</version>
    </dependency>
    <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <scope>runtime</scope>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>${jjwt.version}</version>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-impl</artifactId>
        <version>${jjwt.version}</version>
        <scope>runtime</scope>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-jackson</artifactId>
        <version>${jjwt.version}</version>
        <scope>runtime</scope>
    </dependency>
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <optional>true</optional>
    </dependency>
</dependencies>
```

#### 二、配置文件设置（0.5h）

创建`application.yml`，配置数据库连接、JWT密钥、文件上传路径、MyBatis-Plus等。

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rubber_shop?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: RubberShopJwtSecretKey2025RubberShopJwtSecretKey2025
  expiration: 86400000

upload:
  path: uploads/
```

#### 三、数据库初始化SQL（1.5h）

编写并执行完整建表脚本（15张表），所有字段加`_zj`后缀，包含预置商家、厂家、分类、商品等基础数据。

```sql
-- 预置商家（密码 admin123，BCrypt 加密后示例）
INSERT INTO user_zj (phone_zj, password_zj, role_zj, real_name_zj, balance_zj, credit_limit_zj, status_zj, created_at_zj)
VALUES ('13800000000', '$2a$10$...', 'merchant', '管理员', 0.00, 0.00, 1, NOW());

-- 预置厂家
INSERT INTO user_zj (phone_zj, password_zj, role_zj, company_name_zj, status_zj, created_at_zj)
VALUES ('13900000001', '$2a$10$...', 'factory', '正佳橡胶厂', 1, NOW());

-- 分类与商品示例（略）
```

#### 四、实体类与基础代码生成（2h）

通过MyBatis-Plus代码生成器或手动创建所有实体类、Mapper接口、Service接口及实现类。实体类使用`@TableName`指定表名，`@TableField`映射带后缀字段名。

**基础包结构**：
```
com.rubber.shop/
├── Application.java
├── common/          # Result, BusinessException, GlobalExceptionHandler
├── config/          # Security, MyBatisPlus, WebConfig
├── controller/      # 前台接口
├── controller/admin/# 管理后台接口
├── dto/             # 请求响应对象
├── entity/          # 实体类
├── mapper/          # MyBatis Mapper
├── service/         # 接口
└── service/impl/    # 实现
```

#### 五、静态资源映射（0.5h）

```java
@Configuration
public class WebConfig implements WebMvcConfigurer {
    @Override
    public void addResourceHandlers(ResourceHandlerRegistry registry) {
        registry.addResourceHandler("/uploads/**")
                .addResourceLocations("file:" + System.getProperty("user.dir") + "/uploads/");
    }
}
```

#### 六、项目验证（0.5h）

1. 启动项目，确认无报错
2. 检查数据表是否全部生成
3. 查看Swagger（如有集成）或简单访问测试接口

---

## 第2天：Spring Security + JWT 认证模块

**目标**：实现基于JWT的认证流程，包括登录、注册、Token生成、过滤器、角色权限控制。

**前置条件**：第1天项目可运行，user_zj表已存在。

### 任务详细 (6h)

#### 一、Spring Security配置与JWT工具类（2h）

配置Spring Security禁用默认表单登录，开放注册登录接口，添加JWT认证过滤器，定义角色权限。

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http.csrf().disable()
            .sessionManagement().sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            .and()
            .authorizeHttpRequests()
            .requestMatchers("/api/auth/**", "/uploads/**").permitAll()
            .requestMatchers("/api/admin/**").hasRole("MERCHANT")
            .requestMatchers("/api/factory/**").hasRole("FACTORY")
            .anyRequest().authenticated()
            .and()
            .addFilterBefore(jwtAuthFilter, UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

**JwtUtil 核心方法**：
- `generateToken(userId, role)` 生成JWT，payload中包含userId和role。
- `validateToken(token)` 校验Token并返回Claims。
- `getUserIdFromToken(token)` 从Claims中提取userId。

#### 二、JWT认证过滤器（1h）

编写`JwtAuthFilter`继承`OncePerRequestFilter`，从请求头`Authorization: Bearer <token>`中提取Token，校验后将用户信息设置到`SecurityContext`。

```java
String token = request.getHeader("Authorization");
if (token != null && token.startsWith("Bearer ")) {
    token = token.substring(7);
    if (jwtUtil.validateToken(token)) {
        Long userId = jwtUtil.getUserIdFromToken(token);
        String role = jwtUtil.getRoleFromToken(token);
        // 构造认证对象
        UsernamePasswordAuthenticationToken authentication =
            new UsernamePasswordAuthenticationToken(userId, null, 
                Collections.singletonList(new SimpleGrantedAuthority("ROLE_" + role.toUpperCase())));
        SecurityContextHolder.getContext().setAuthentication(authentication);
    }
}
```

#### 三、登录与注册接口（1.5h）

构建`AuthController`，编写`POST /api/auth/login`和`POST /api/auth/register`。

**登录逻辑**：
- 查询user_zj表匹配手机号
- BCrypt密码比对
- 生成JWT并返回，同时返回用户基本信息（不含密码）

**注册逻辑**：
- 检查手机号唯一性
- 密码BCrypt加密
- 插入用户记录，默认角色`customer`，余额0

```java
@PostMapping("/login")
public Result<LoginResponse> login(@RequestBody @Valid LoginRequest req) {
    User user = userService.getByPhone(req.getPhone());
    if (user == null || !passwordEncoder.matches(req.getPassword(), user.getPassword_zj())) {
        throw new BusinessException("手机号或密码错误");
    }
    String token = jwtUtil.generateToken(user.getId_zj(), user.getRole_zj());
    // 构建响应
    LoginResponse resp = new LoginResponse();
    resp.setToken(token);
    resp.setUserId(user.getId_zj());
    resp.setRole(user.getRole_zj());
    return Result.success("登录成功", resp);
}
```

#### 四、获取当前用户信息接口（0.5h）

`GET /api/auth/profile`：通过SecurityContext中的userId查询用户信息并返回。

#### 五、功能验证（1h）

使用Apifox或Postman测试：
1. 注册新顾客，返回成功
2. 登录商家账号，获取JWT
3. 携带JWT访问需要认证的接口（如`/api/user/profile`）
4. 无Token或Token错误时返回401

---

## 第3天：用户管理、地址、充值、修改密码

**目标**：完善用户模块，包括商家后台管理用户、顾客修改密码、地址维护、余额充值、流水记录。

**前置条件**：认证模块完成，JWT可正常使用。

### 任务详细 (6h)

#### 一、管理员用户管理接口（2h）

在`AdminUserController`中实现：

- `GET /api/admin/users` — 分页查询用户，支持手机号搜索、角色筛选，返回用户列表（密码字段脱敏）。
- `GET /api/admin/users/{id}` — 用户详情，包含余额、赊账额度等。
- `PUT /api/admin/users/{id}` — 修改用户余额、赊账额度、状态，记录余额变动日志（type=admin_adjust）。

**调额逻辑**：支持增减式，传入正负值。使用乐观锁更新余额，并写入`balance_log_zj`。

```java
@PutMapping("/{id}/balance")
public Result<?> adjustBalance(@PathVariable Long id, @RequestBody Map<String, BigDecimal> body) {
    BigDecimal amount = body.get("amount");
    // 更新余额，乐观锁
    LambdaUpdateWrapper<User> wrapper = new LambdaUpdateWrapper<>();
    wrapper.eq(User::getId_zj, id)
           .setSql("balance_zj = balance_zj + " + amount)
           .ge(amount.compareTo(BigDecimal.ZERO) < 0, User::getBalance_zj, amount.abs());
    boolean success = userService.update(wrapper);
    if (!success) throw new BusinessException("余额不足，调整失败");
    // 记录日志
    ...
}
```

#### 二、顾客个人中心接口（1.5h）

- `PUT /api/auth/password` — 修改密码（需提供旧密码，校验后加密存储）。
- `GET /api/user/profile` — 获取当前用户信息。
- `PUT /api/user/profile` — 修改个人资料（姓名、头像、默认收货地址）。
- `POST /api/user/recharge` — 顾客自行充值，增加余额，记录日志type=recharge。
- `GET /api/user/balance_logs` — 分页查询当前用户余额变动记录。

#### 三、地址快照验证（1h）

下单前必须校验地址完整性，顾客可通过修改资料更新默认地址，下单时快照到订单表。

#### 四、全局异常处理优化（0.5h）

统一返回格式`Result`，处理`@Valid`参数校验异常，返回友好错误信息。

#### 五、接口测试（1h）

测试管理员调额（正负值）、顾客充值、修改密码、地址更新等，验证余额流水记录是否正确。

---

## 第4天：分类管理、商品管理（含图片上传）

**目标**：实现分类树维护、商品CRUD、图片上传、多图管理。

**前置条件**：category_zj、product_zj 表已就绪，文件上传配置完成。

### 任务详细 (6h)

#### 一、分类管理（1.5h）

**公开接口**：`GET /api/categories` 返回树形结构。

**管理接口**（`AdminCategoryController`）：
- 新增分类：支持无限级，父级ID可选
- 修改分类名称、排序
- 删除分类（无子分类且无关联商品）

```java
@PostMapping
public Result<?> add(@RequestBody Category category) {
    categoryService.save(category);
    return Result.success("分类添加成功", null);
}
```

#### 二、商品CRUD（2.5h）

**顾客端**：`GET /api/products` 分页查询已上架商品，支持按分类、名称搜索；`GET /api/products/{id}` 查看详情（含多图）。

**管理端**（`AdminProductController`）：
- 查看全部商品（含下架商品）
- 新增商品：接收主图URL + 多图JSON数组，自动存储到product表（images字段为JSON）
- 修改商品：更新基础信息，级联更新多图
- 下架/删除：逻辑删除，状态改为off

#### 三、图片上传接口（1h）

`POST /api/upload`：接收`MultipartFile`，保存到本地`uploads/`目录，返回可访问URL `http://localhost:8080/uploads/xxx.jpg`。

```java
@PostMapping("/upload")
public Result<String> upload(@RequestParam("file") MultipartFile file) {
    String fileName = UUID.randomUUID() + "." + FilenameUtils.getExtension(file.getOriginalFilename());
    File dest = new File(uploadPath + fileName);
    file.transferTo(dest);
    return Result.success("上传成功", "/uploads/" + fileName);
}
```

#### 四、功能验证（1h）

测试图片上传返回URL，新增商品时图片数组存储格式，分类树正确展示，编辑商品后信息更新。

---

## 第5天：订单模块（下单、支付）

**目标**：实现顾客下单、余额支付/赊账支付、库存扣减（带乐观锁）、订单生成与支付状态更新。

**前置条件**：商品库存操作、用户余额/赊账额度可用，地址快照机制就绪。

### 任务详细 (6h)

#### 一、下单接口（2h）

`POST /api/orders`：接收商品列表（productId, quantity）、支付方式（balance/credit）、可选收货地址（为空则使用默认地址）。
**核心流程**（事务内）：
1. 校验所有商品存在且上架
2. 检查库存充足（乐观锁扣减）
3. 插入`sale_out`类型库存流水
4. 地址快照处理（若前端未传，从用户默认地址获取并校验非空）
5. 计算总金额，生成订单主记录和明细（状态初始为paid，因为立即支付）
6. 根据支付方式执行支付逻辑

```java
@Transactional
public Order createOrder(Long userId, List<OrderItemRequest> items, String paymentMethod, String address) {
    // 库存检查并扣减
    for (OrderItemRequest item : items) {
        Product product = productMapper.selectById(item.getProductId());
        if (product == null || !"on".equals(product.getStatus_zj())) throw new BusinessException("商品已下架或不存在");
        // 乐观锁扣库存
        int rows = productMapper.decreaseStock(product.getId_zj(), item.getQuantity());
        if (rows == 0) throw new BusinessException("商品[" + product.getName_zj() + "]库存不足");
        // 记录库存流水
        stockLogService.saveLog(product.getId_zj(), -item.getQuantity(), ...);
    }
    // 创建订单 ...
}
```

#### 二、支付逻辑（2h）

**余额支付**：
- 验证用户余额 >= 订单总额
- 乐观锁扣减余额
- 记录余额流水（type=consume）
- 订单状态设为paid

**赊账支付**：
- 计算用户可用赊账额度：`credit_limit - 当前未清应收总额`
- 额度不足则拒绝
- 创建应收记录（`receivable_zj`）
- 订单状态设为paid

**支付后日志**：写订单状态变更日志。

#### 三、订单查询接口（1h）

- `GET /api/orders`：顾客查看自己的订单，按状态筛选、分页。
- `GET /api/orders/{id}`：订单详情（含商品明细、物流信息、状态日志）。

#### 四、功能验证（1h）

使用不同支付方式下单，测试库存不足、余额不足、赊账超限等异常场景，检查日志完整性。

---

## 第6天：订单管理（接单、发货、收货、取消/退款）

**目标**：实现商家接单、发货，顾客确认收货，订单取消与退款流程。

**前置条件**：订单状态流转定义清晰，库存恢复、余额退款逻辑准备就绪。

### 任务详细 (6h)

#### 一、商家接单与发货（2h）

**接单** `PUT /api/orders/{id}/accept`：
- 校验订单状态为paid
- 更新状态为accepted，记录日志

**发货** `PUT /api/orders/{id}/ship`：
- 状态为accepted或paid（兼容）
- 填写物流公司、快递单号
- 状态更新为shipped，记录发货时间

```java
public void shipOrder(Long orderId, String expressCompany, String trackingNo) {
    Order order = orderMapper.selectById(orderId);
    if (order == null || !(order.getStatus_zj().equals("paid") || order.getStatus_zj().equals("accepted")))
        throw new BusinessException("订单状态不允许发货");
    order.setStatus_zj("shipped");
    order.setExpress_company_zj(expressCompany);
    order.setTracking_no_zj(trackingNo);
    order.setShipped_at_zj(LocalDateTime.now());
    orderMapper.updateById(order);
    statusLogService.addLog(orderId, oldStatus, "shipped", operatorId);
}
```

#### 二、确认收货（0.5h）

`PUT /api/orders/{id}/receive`：状态为shipped，直接变更为completed，记录完成时间。顾客或商家均可操作。

#### 三、取消订单与退款（2h）

`POST /api/orders/{id}/cancel`：
- 仅允许状态为paid或accepted（未发货）
- 顾客只能取消自己的订单，商家可取消任何订单

**退款处理**：
- 余额支付：退回余额，记录余额流水（type=refund）
- 赊账支付：作废应收记录（若已有部分还款则退还至余额）
- 库存回加，记录refund_in类型库存流水
- 订单状态更新为refunded

#### 四、状态日志完善（0.5h）

确保所有状态变更都写入`order_status_log_zj`，记录操作人、旧状态、新状态、备注。

#### 五、功能测试（1h）

测试发货、收货、取消订单等全流程，检查退款金额、库存恢复是否正确。

---

## 第7天：定制模块（顾客提交定制、商家报价、确认生成订单）

**目标**：实现定制需求的提交、商家报价、顾客确认并自动生成订单的完整闭环。

**前置条件**：customization_zj、customization_item_zj表已存在，文件上传接口可用。

### 任务详细 (6h)

#### 一、顾客提交定制（1.5h）

`POST /api/customizations`：
- 接收需求描述文本、可选参考图片（通过上传获取URL）
- 保存定制主记录，状态pending_quote
- 图片URL以JSON数组存储

#### 二、商家报价（1.5h）

`PUT /api/customizations/{id}/quote`：
- 添加多个报价明细（spec、quantity、unitPrice）
- 计算总报价，更新定制状态为quoted

#### 三、顾客确认（2h）

`PUT /api/customizations/{id}/confirm`：
- 顾客选择支付方式
- **自动生成临时商品**：为每个报价项创建商品记录（is_customized=1，status=off，库存等可设1）
- **生成订单**：调用下单逻辑，支付时需特殊处理（如库存检查可通过）
- 定制关联订单ID，状态改为confirmed

#### 四、定制商品转常规（1h）

`POST /api/customizations/{id}/convert-to-product`：商家可将定制商品转为正常商品，设置库存、分类等信息。

#### 五、功能测试（0.5h）

提交定制需求，商家报价，顾客确认后检查订单和商品生成情况。

---

## 第8天：采购模块（商家创建采购单、厂家接单发货、入库）

**目标**：实现采购单创建、修改、厂家操作状态、确认收货入库。

**前置条件**：purchase_zj、purchase_item_zj、stock_log_zj表已就绪。

### 任务详细 (6h)

#### 一、商家创建与修改采购单（2h）

**创建** `POST /api/purchases`：
- 指定厂家、预计交货日期、明细列表
- 明细可暂不关联product_id，后续修改

**修改** `PUT /api/purchases/{id}`：
- 仅限状态为pending的采购单
- 可修改基本信息、更换明细（补全product_id）
- 重新计算总金额

#### 二、厂家移动端接口（2h）

**查看采购单**：`GET /api/purchases` 厂家仅看到自己的采购单（通过SecurityContext中的role过滤）
**操作状态**：`PUT /api/purchases/{id}/status`
- `pending` → `confirmed`：厂家确认接单
- `confirmed` → `shipped`：厂家发货，填写物流信息

#### 三、商家确认收货与入库（1.5h）

`PUT /api/purchases/{id}/receive`（商家）：
- 状态需为shipped
- 遍历采购明细，对已关联product_id的项更新商品库存，记录stock_log (type=purchase_in)
- 若存在未关联商品的明细，则拒绝收货，提示错误

```java
for (PurchaseItem item : purchaseItems) {
    if (item.getProduct_id_zj() == null) {
        throw new BusinessException("明细[" + item.getProduct_name_zj() + "]未关联商品，无法入库");
    }
    productMapper.increaseStock(item.getProduct_id_zj(), item.getQuantity_zj());
    stockLogService.saveLog(item.getProduct_id_zj(), item.getQuantity_zj(), ...);
}
```

#### 四、功能测试（0.5h）

测试采购流程：创建采购单（明细未关联商品）→修改补全商品→厂家接单发货→商家收货入库，验证库存增加和日志。

---

## 第9天：赊账与还款（应收收款管理）

**目标**：实现赊账应收查询、顾客主动还款、商家登记线下收款。

**前置条件**：receivable_zj、receipt_zj表已存在，余额流水机制完善。

### 任务详细 (6h)

#### 一、应收款列表与详情（1.5h）

- `GET /api/receivables`：顾客查看自己的应收，商家查看全部
- `GET /api/receivables/{id}`：详情含还款记录

#### 二、还款接口（2.5h）

`POST /api/receivables/{id}/receipts`：
- **顾客还款**：仅支持balance方式，扣减余额，记录balance_log (type=repay)
- **商家登记线下收款**：cash或bank_transfer，不涉及余额变动，仅记录收款
- 更新应收的已还金额和状态（部分还清/还清）
- 记录receipt收款记录

```java
if ("balance".equals(paymentMethod) && currentUserRole == "customer") {
    // 扣减余额
    userService.decreaseBalance(userId, amount);
    balanceLogService.log(userId, -amount, "repay", ...);
}
receivable.setAmount_paid_zj(receivable.getAmount_paid_zj().add(amount));
if (receivable.getAmount_paid_zj().compareTo(receivable.getAmount_owed_zj()) >= 0) {
    receivable.setStatus_zj("paid");
} else {
    receivable.setStatus_zj("partially_paid");
}
```

#### 三、逾期提醒（可选，0.5h）

可增加定时任务或手动触发检查due_date过期的应收记录，但不强制实现。

#### 四、功能测试（1h）

模拟赊账下单后，顾客还款、商家线下收款，验证余额变动、应收状态更新是否正确。

---

## 第10天：库存管理完善、数据统计与系统配置

**目标**：完成库存日志查询、预警，增加管理后台统计数据接口，实现系统配置管理。

**前置条件**：库存逻辑已基本完成，订单、采购等数据已积累。

### 任务详细 (6h)

#### 一、库存日志分页查询（1h）

`GET /api/stock/logs`：支持按商品名称、类型筛选，分页返回流水明细。

#### 二、数据统计接口（2h）

`GET /api/admin/statistics/dashboard`：
- 今日/本周销售额、订单量
- 近7天销售趋势（按日聚合）
- 热销商品Top5
- 库存预警商品数量

```sql
SELECT DATE(created_at_zj) as date, SUM(actual_amount_zj) as total
FROM order_zj WHERE status_zj = 'completed' AND created_at_zj >= ...
GROUP BY DATE(created_at_zj)
```

#### 三、系统配置管理（1.5h）

- `GET /api/configs`：公开获取配置项（如站点名称）
- `PUT /api/configs/{key}`：管理员修改配置值，实时生效

```java
@RestController
@RequestMapping("/api/configs")
public class ConfigController {
    @GetMapping
    public Result<Map<String, String>> list() { ... }
  
    @PutMapping("/{key}")
    public Result update(@PathVariable String key, @RequestBody Map<String, String> body) {
        sysConfigService.updateValue(key, body.get("value"));
        return Result.success("配置已更新");
    }
}
```

#### 四、库存预警与手动调整（1h，可与上面合并）

- `GET /api/stock/warnings`：库存低于预警阈值的商品列表
- `POST /api/stock/adjust`：管理员手动调整库存（正负值），记录manual_in/out日志

#### 五、功能测试（0.5h）

验证仪表盘数据准确性，配置修改后前端立即生效，预警列表正确。

---

## 第11天：Vue管理后台项目搭建、登录、仪表盘

**目标**：初始化Vue3 + Element Plus管理后台，实现登录鉴权、路由守卫、仪表盘数据展示。

**前置条件**：后端API已完成，理解JWT认证流程。

### 任务详细 (6h)

#### 一、项目初始化（1h）

使用Vite创建Vue3项目，安装vue-router、pinia、axios、element-plus、echarts等依赖。

#### 二、工具封装（1.5h）

- **axios封装**：自动携带JWT（从localStorage获取），响应拦截处理401跳转登录
- **Pinia store**：存储用户信息和token，提供登录/退出方法
- **路由配置**：各业务页面路由，动态菜单根据角色显示
- **路由守卫**：判断token是否存在，否则跳转登录

#### 三、登录页面（1h）

实现手机号密码登录表单，调用登录接口，存储token到localStorage，获取用户信息后跳转仪表盘。

```javascript
const login = async () => {
  const res = await api.post('/auth/login', { phone, password })
  const { token, userId, role } = res.data
  localStorage.setItem('token', token)
  userStore.setUserInfo({ userId, role })
  router.push('/dashboard')
}
```

#### 四、仪表盘页面（2h）

对接统计接口，使用ECharts绘制销售趋势折线图、热销商品柱状图，展示关键数据卡片。

#### 五、基础布局验证（0.5h）

测试登录、退出、菜单导航、仪表盘数据展示。

---

## 第12天：管理后台商品与分类管理页面

**目标**：完成后台商品列表、新增/编辑商品、分类树管理页面。

**前置条件**：管理后台基础框架可用，API接口已开发。

### 任务详细 (6h)

#### 一、商品列表页（2h）

使用Element Plus的Table组件展现商品列表，支持分页、按名称搜索、按分类筛选、状态切换上架/下架。列表操作栏提供编辑、下架/删除按钮。

#### 二、商品新增/编辑页（2.5h）

表单包含：
- 基本信息：名称、分类下拉树、描述、图片上传（支持多图预览）
- 价格、库存、预警值
- 关联厂家选择
- 规格JSON编辑器（可选）
- 提交时调用新增或修改接口

#### 三、分类管理页（1.5h）

使用Element Plus的Tree组件展示分类树，添加节点、修改名称、删除确认（有无子节点/商品），数据与后端同步。

#### 四、功能验证

测试新增商品、上传图片、分类增删改等操作。

---

## 第13天：管理后台订单、定制、采购、库存、用户管理页面

**目标**：实现后台订单管理、定制管理、采购管理、库存管理、用户管理等全部业务页面。

**前置条件**：后台框架已搭建，相关API可用。

### 任务详细 (6h)

#### 一、订单管理（2h）

- 订单列表页：状态Tab切换（全部/已支付/已发货/已完成/已取消等），搜索框，表格显示订单核心信息。
- 订单详情页：可查看商品明细、收货地址、物流信息、状态时间线、操作按钮（接单、发货、取消等）。发货操作为弹出对话框填写物流。

#### 二、定制管理（1h）

列表展示所有定制需求，详情页可进行报价（多项规格输入）、确认接单、转为常规商品等操作。

#### 三、采购管理（1h）

采购单列表，新增/编辑采购单（选择厂家、添加明细），操作收货等。

#### 四、库存管理（1h）

库存日志列表查询、库存预警表格、手动出入库表单。

#### 五、用户管理（1h）

用户列表，点击可查看详情，支持调整余额/赊账额度（类似弹窗输入正负值）。

---

## 第14天：UniApp移动端项目搭建与顾客端核心功能（商品浏览、下单、订单）

**目标**：创建UniApp项目，实现顾客端底部导航、商品浏览、搜索、下单、确认收货、还款等核心功能。

**前置条件**：后端接口全部就绪，HBuilderX可用。

### 任务详细 (6h)

#### 一、项目初始化与配置（1.5h）

- 创建UniApp（Vue3）项目
- 配置TabBar（首页、定制、订单、我的）
- 封装请求模块（自动携带Token）
- Pinia状态管理（用户信息、购物车等）
- 安装uni-ui等组件库

#### 二、登录/注册与个人中心（1h）

- 登录页：手机号+密码登录，调用接口后存储token，跳转首页
- 我的页面：显示余额、赊账额度，提供充值入口、修改密码、地址管理等功能

#### 三、商品浏览与搜索（1h）

- 首页展示分类，点击进入商品列表，支持分页加载、搜索
- 商品详情页：展示图片轮播、价格、库存、添加到购物车（或直接下单）

#### 四、下单流程（1.5h）

- 购物车/立即购买页面：选择支付方式，显示默认收货地址（可修改），提交订单
- 下单成功后可查看订单详情
- 订单列表页：状态筛选，查看详情

#### 五、订单操作（1h）

在订单详情页提供支付（若未付）、确认收货（按钮）、取消订单（仅未发货状态）等操作，调用相应API。

---

## 第15天：UniApp移动端定制、采购（商家/厂家端）、全面联调与演示准备

**目标**：完成移动端所有角色功能，进行全链路测试并修复问题，准备答辩。

**前置条件**：移动端框架已搭建，多角色登录逻辑已完成。

### 任务详细 (6h)

#### 一、顾客端定制功能（1h）

- 定制页面：填写需求描述，上传参考图片，提交定制
- 定制列表：查看状态，待报价/已报价等，确认报价页面（选择支付方式，自动生成订单）

#### 二、商家移动端（1.5h）

- TabBar调整：商品、订单、我的
- 商品浏览：可查看全部商品（包括下架），支持搜索
- 订单处理：查看所有订单，操作接单、发货

#### 三、厂家移动端（1.5h）

- 底部导航：采购单、我的
- 采购单列表：仅显示自己的采购单，点击可查看详情
- 操作接单、发货：对应状态按钮，可填写物流信息

#### 四、全链路联调（1.5h）

核心业务：顾客注册→充值→浏览商品下单（余额/赊账）→支付→商家接单发货→顾客确认收货→赊账单还款→商家查看财务报表。
定制流程：提交定制→商家报价→顾客确认生成订单→发货→完成。
采购流程：商家创建采购单→厂家接单发货→商家收货入库→库存更新。
异常场景：库存不足、余额不足、赊账超额、未发货取消等。

#### 五、Bug修复与演示准备（0.5h）

快速修复联调发现的关键问题，整理演示要点，确保项目可流畅演示。

---

*祝开发顺利，答辩高分！*
```