# 橡胶实体店进销存管理系统开发任务逐日拆解（基于V13.0设计文档）

> 本拆解依据《橡胶实体店进销存管理系统（V13.0 明文密码+Android客户端版）》编写，将全部功能分解为15天（每天6小时）的详细任务。开发者可按实际情况调整天数，重点在于清晰掌握每天的目标、工作内容和检验标准。

> 技术栈：Spring Boot 3 + MyBatis-Plus + Spring Security + JWT（密码明文存储） + MySQL + Vue3 + Element Plus + Android（原生）
> API测试：Apifox  数据库字段统一附加 `_zj` 后缀

---

## 第1天：后端项目初始化与数据库基础

**目标**：创建Spring Boot项目，搭建数据库，生成基础代码结构，插入预置数据。

**前置条件**：已安装JDK17、MySQL8、IDEA等开发工具，手动创建数据库`rubber_shop`。

### 任务详细 (6h)

#### 一、创建Maven项目（1h）

通过Spring Initializr或手动创建Maven项目，添加以下依赖：web、mysql、mybatis-plus、validation、spring-security、jjwt、lombok。**本版密码明文存储，不需要加密相关依赖。**

**pom.xml 核心配置**：

```xml
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
</parent>

<properties>
    <java.version>17</java.version>
    <mybatis-plus.version>3.5.7</mybatis-plus.version>
    <jjwt.version>0.12.3</jjwt.version>
</properties>

<dependencies>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        <version>${mybatis-plus.version}</version>
    </dependency>
    <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <scope>runtime</scope>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>${jjwt.version}</version>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-impl</artifactId>
        <version>${jjwt.version}</version>
        <scope>runtime</scope>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-jackson</artifactId>
        <version>${jjwt.version}</version>
        <scope>runtime</scope>
    </dependency>
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <optional>true</optional>
    </dependency>
</dependencies>
```

#### 二、配置文件设置（0.5h）

创建`application.yml`，配置数据库连接、JWT密钥、文件上传路径、MyBatis-Plus等。

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rubber_shop?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: RubberShopJwtSecretKey2025RubberShopJwtSecretKey2025
  expiration: 86400000

upload:
  path: uploads/
```

#### 三、数据库初始化SQL（1.5h）

编写并执行完整建表脚本（15张表），所有字段加`_zj`后缀，预置数据中**密码使用明文**。

```sql
-- 预置商家（密码 admin123，明文）
INSERT INTO user_zj (phone_zj, password_zj, role_zj, real_name_zj, balance_zj, credit_limit_zj, status_zj, created_at_zj)
VALUES ('13800000000', 'admin123', 'merchant', '管理员', 0.00, 0.00, 1, NOW());

-- 预置厂家（密码 factory123，明文）
INSERT INTO user_zj (phone_zj, password_zj, role_zj, company_name_zj, status_zj, created_at_zj)
VALUES ('13900000001', 'factory123', 'factory', '正佳橡胶厂', 1, NOW());

-- 分类与商品示例
INSERT INTO category_zj (name_zj, parent_id_zj, sort_zj, created_at_zj) VALUES 
('橡胶原料', 0, 1, NOW()), ('橡胶制品', 0, 2, NOW());
INSERT INTO product_zj (category_id_zj, name_zj, price_zj, stock_zj, status_zj, created_at_zj) 
VALUES (1, '天然橡胶片', 25.00, 500, 'on', NOW());
```

#### 四、实体类与基础代码生成（2h）

通过MyBatis-Plus代码生成器或手动创建所有实体类、Mapper接口、Service接口及实现类。实体类使用`@TableName`指定表名，`@TableField`映射带后缀字段名。

**基础包结构**：
```
com.rubber.shop/
├── Application.java
├── common/          # Result, BusinessException, GlobalExceptionHandler
├── config/          # Security, MyBatisPlus, WebConfig
├── controller/      # 前台接口
├── controller/admin/# 管理后台接口
├── dto/             # 请求响应对象
├── entity/          # 实体类
├── mapper/          # MyBatis Mapper
├── service/         # 接口
└── service/impl/    # 实现
```

#### 五、静态资源映射（0.5h）

```java
@Configuration
public class WebConfig implements WebMvcConfigurer {
    @Override
    public void addResourceHandlers(ResourceHandlerRegistry registry) {
        registry.addResourceHandler("/uploads/**")
                .addResourceLocations("file:" + System.getProperty("user.dir") + "/uploads/");
    }
}
```

#### 六、项目验证（0.5h）

1. 启动项目，确认无报错
2. 检查数据表是否全部生成，预置用户密码为明文
3. 浏览器访问静态资源测试

---

## 第2天：Spring Security + JWT 认证模块（明文密码版）

**目标**：实现基于JWT的认证流程，包括登录、注册、Token生成、过滤器、角色权限控制。**密码明文存储与比对，不使用任何加密。**

**前置条件**：第1天项目可运行，user_zj表已存在。

### 任务详细 (6h)

#### 一、Spring Security配置与JWT工具类（2h）

配置Spring Security禁用默认表单登录，开放注册登录接口，添加JWT认证过滤器，定义角色权限。**PasswordEncoder 使用 NoOpPasswordEncoder，不对密码做任何加密。**

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http.csrf().disable()
            .sessionManagement().sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            .and()
            .authorizeHttpRequests()
            .requestMatchers("/api/auth/register", "/api/auth/login", "/uploads/**").permitAll()
            .requestMatchers("/api/admin/**").hasRole("MERCHANT")
            .requestMatchers("/api/factory/**").hasRole("FACTORY")
            .anyRequest().authenticated()
            .and()
            .addFilterBefore(jwtAuthFilter, UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return NoOpPasswordEncoder.getInstance();  // 明文，不加密
    }
}
```

**JwtUtil** 核心方法：
- `generateToken(userId, role)` 生成JWT。
- `validateToken(token)` 校验Token并返回Claims。
- `getUserIdFromToken(token)` 从Claims中提取userId。

#### 二、JWT认证过滤器（1h）

编写`JwtAuthFilter`继承`OncePerRequestFilter`，从请求头`Authorization: Bearer <token>`中提取Token，校验后将用户信息设置到`SecurityContext`。

```java
String token = request.getHeader("Authorization");
if (token != null && token.startsWith("Bearer ")) {
    token = token.substring(7);
    if (jwtUtil.validateToken(token)) {
        Long userId = jwtUtil.getUserIdFromToken(token);
        String role = jwtUtil.getRoleFromToken(token);
        UsernamePasswordAuthenticationToken authentication =
            new UsernamePasswordAuthenticationToken(userId, null, 
                Collections.singletonList(new SimpleGrantedAuthority("ROLE_" + role.toUpperCase())));
        SecurityContextHolder.getContext().setAuthentication(authentication);
    }
}
```

#### 三、登录与注册接口（1.5h）

构建`AuthController`，编写`POST /api/auth/register`和`POST /api/auth/login`。

```java
@RestController
@RequestMapping("/api/auth")
public class AuthController {
    @PostMapping("/register")
    public Result<LoginResponse> register(@RequestBody @Valid RegisterRequest req) {
        if (userService.phoneExists(req.getPhone())) {
            throw new BusinessException("手机号已注册");
        }
        User user = new User();
        user.setPhone_zj(req.getPhone());
        user.setPassword_zj(req.getPassword()); // 明文存储
        user.setRole_zj("customer");
        user.setBalance_zj(BigDecimal.ZERO);
        userService.save(user);
        String token = jwtUtil.generateToken(user.getId_zj(), user.getRole_zj());
        return Result.success("注册成功", new LoginResponse(token, user.getId_zj(), user.getRole_zj()));
    }

    @PostMapping("/login")
    public Result<LoginResponse> login(@RequestBody @Valid LoginRequest req) {
        User user = userService.getByPhone(req.getPhone());
        if (user == null || !req.getPassword().equals(user.getPassword_zj())) {
            throw new BusinessException("手机号或密码错误");
        }
        String token = jwtUtil.generateToken(user.getId_zj(), user.getRole_zj());
        return Result.success("登录成功", new LoginResponse(token, user.getId_zj(), user.getRole_zj()));
    }
}
```

#### 四、获取当前用户信息接口（0.5h）

`GET /api/auth/profile`：通过SecurityContext中的userId查询用户信息并返回（密码字段置空）。

#### 五、功能验证（1h）

使用Apifox测试：
1. 注册新顾客，返回成功
2. 登录商家账号，获取JWT
3. 携带JWT访问受保护接口
4. 无Token或Token错误时返回401
5. 检查数据库密码字段为明文

---

## 第3天：用户管理、地址、充值、修改密码

**目标**：完善用户模块，包括商家后台管理用户、顾客修改密码、地址维护、余额充值、流水记录。

**前置条件**：认证模块完成，JWT可正常使用。

### 任务详细 (6h)

#### 一、管理员用户管理接口（2h）

在`AdminUserController`中实现：
- `GET /api/admin/users`：分页查询用户，支持手机号搜索、角色筛选，密码字段脱敏。
- `GET /api/admin/users/{id}`：用户详情（余额、赊账额度）。
- `PUT /api/admin/users/{id}`：修改余额/额度/状态，记录balance_log (type=admin_adjust)。

使用乐观锁更新余额：

```java
@PutMapping("/{id}/balance")
public Result<?> adjustBalance(@PathVariable Long id, @RequestBody Map<String, BigDecimal> body) {
    BigDecimal amount = body.get("amount");
    LambdaUpdateWrapper<User> wrapper = new LambdaUpdateWrapper<>();
    wrapper.eq(User::getId_zj, id)
           .setSql("balance_zj = balance_zj + " + amount)
           .ge(amount.compareTo(BigDecimal.ZERO) < 0, User::getBalance_zj, amount.abs());
    boolean success = userService.update(wrapper);
    if (!success) throw new BusinessException("余额不足，调整失败");
    // 记录 balance_log
}
```

#### 二、顾客个人中心接口（1.5h）

- `PUT /api/auth/password`：提供旧密码（明文验证），新密码明文存储。
- `GET /api/user/profile`：获取当前用户信息。
- `PUT /api/user/profile`：修改资料（姓名、头像、默认收货地址）。
- `POST /api/user/recharge`：充值，增加余额，记录日志type=recharge。
- `GET /api/user/balance_logs`：分页查询余额流水。

#### 三、地址快照验证（1h）

下单前校验地址完整性，顾客修改默认地址后，下单时快照到订单表。

#### 四、全局异常处理优化（0.5h）

统一`Result`返回格式，处理`@Valid`参数校验异常。

#### 五、接口测试（1h）

测试管理员调额、顾客充值、修改密码、地址更新，验证余额流水记录。

---

## 第4天：分类管理、商品管理（含图片上传）

**目标**：实现分类树维护、商品CRUD、图片上传、多图管理。

**前置条件**：category_zj、product_zj 表已就绪，文件上传配置完成。

### 任务详细 (6h)

#### 一、分类管理（1.5h）

- 公开接口：`GET /api/categories` 返回树形分类。
- 管理接口（`AdminCategoryController`）：新增、修改、删除（校验无子分类且无关联商品）。

```java
@RestController
@RequestMapping("/api/admin/categories")
public class AdminCategoryController {
    @PostMapping
    public Result<?> add(@RequestBody Category category) { ... }
    @PutMapping("/{id}")
    public Result<?> update(@PathVariable Long id, @RequestBody Category category) { ... }
    @DeleteMapping("/{id}")
    public Result<?> delete(@PathVariable Long id) {
        // 校验：是否有子分类或关联商品
    }
}
```

#### 二、商品CRUD（2.5h）

- 顾客端：`GET /api/products` 分页查询上架商品，支持搜索；`GET /api/products/{id}` 详情。
- 管理端（`AdminProductController`）：增删改查，多图JSON存储，状态切换。

#### 三、图片上传接口（1h）

`POST /api/upload`：接收`MultipartFile`，保存到`uploads/`目录，返回URL。

```java
@PostMapping("/upload")
public Result<String> upload(@RequestParam("file") MultipartFile file) {
    String fileName = UUID.randomUUID() + "." + FilenameUtils.getExtension(file.getOriginalFilename());
    File dest = new File(uploadPath + fileName);
    file.transferTo(dest);
    return Result.success("上传成功", "/uploads/" + fileName);
}
```

#### 四、功能验证（1h）

测试图片上传、商品新增（多图JSON）、分类树展示、编辑更新。

---

## 第5天：订单模块（下单、支付）

**目标**：实现顾客下单、余额支付/赊账支付、库存扣减（乐观锁）、订单生成。

**前置条件**：商品库存、用户余额/赊账额度可用，地址快照机制就绪。

### 任务详细 (6h)

#### 一、下单接口（2h）

`POST /api/orders`：商品列表、支付方式、可选地址。事务内处理：

1. 校验商品存在且上架
2. 乐观锁扣库存，记录`sale_out`库存流水
3. 地址快照
4. 生成订单明细和主记录（状态`paid`）
5. 执行支付

```java
@Transactional
public Order createOrder(Long userId, List<OrderItemRequest> items, String paymentMethod, String address) {
    for (OrderItemRequest item : items) {
        Product product = productMapper.selectById(item.getProductId());
        if (product == null || !"on".equals(product.getStatus_zj())) throw new BusinessException("商品已下架");
        int rows = productMapper.decreaseStock(product.getId_zj(), item.getQuantity());
        if (rows == 0) throw new BusinessException("商品[" + product.getName_zj() + "]库存不足");
        stockLogService.saveLog(product.getId_zj(), -item.getQuantity(), "sale_out", ...);
    }
    // 创建订单 ...
}
```

#### 二、支付逻辑（2h）

- **余额支付**：验证余额≥总额，乐观锁扣减余额，记录`consume`流水。
- **赊账支付**：计算可用额度（`credit_limit - 未清应收`），不足则拒绝，创建`receivable`记录。

#### 三、订单查询接口（1h）

`GET /api/orders`（顾客看自己，商家看全部）、`GET /api/orders/{id}`（含明细和状态日志）。

#### 四、功能验证（1h）

测试不同支付方式下单，验证库存不足、余额不足、赊账超限等异常场景。

---

## 第6天：订单管理（接单、发货、收货、取消/退款）

**目标**：实现商家接单、发货，顾客确认收货，订单取消与退款流程。

### 任务详细 (6h)

#### 一、商家接单与发货（2h）

- 接单：`paid` → `accepted`，记录日志。
- 发货：填写物流，`accepted` 或 `paid` → `shipped`。

```java
public void shipOrder(Long orderId, String expressCompany, String trackingNo) {
    Order order = orderMapper.selectById(orderId);
    if (!order.getStatus_zj().equals("paid") && !order.getStatus_zj().equals("accepted"))
        throw new BusinessException("订单状态不允许发货");
    order.setStatus_zj("shipped");
    order.setExpress_company_zj(expressCompany);
    order.setTracking_no_zj(trackingNo);
    order.setShipped_at_zj(LocalDateTime.now());
    orderMapper.updateById(order);
    statusLogService.addLog(orderId, oldStatus, "shipped", operatorId);
}
```

#### 二、确认收货（0.5h）

`PUT /api/orders/{id}/receive`：`shipped` → `completed`，记录完成时间。

#### 三、取消订单与退款（2h）

仅允许`paid`或`accepted`状态取消，顾客只能取消自己的，商家可取消任何。

- 余额支付：退回余额，记录`refund`流水
- 赊账支付：作废应收（若部分还款则退至余额）
- 库存恢复，记录`refund_in`库存流水
- 状态变为`refunded`

#### 四、状态日志完善（0.5h）

所有状态变更写入`order_status_log_zj`。

#### 五、功能测试（1h）

测试发货、收货、取消订单全流程，退款金额、库存恢复正确。

---

## 第7天：定制模块（顾客提交定制、商家报价、确认生成订单）

**目标**：实现定制需求的提交、商家报价、顾客确认并自动生成订单的完整闭环。

**前置条件**：customization_zj、customization_item_zj表已存在，文件上传接口可用。

### 任务详细 (6h)

#### 一、顾客提交定制（1.5h）

`POST /api/customizations`：接收描述文本、参考图片URL（JSON数组），状态`pending_quote`。

#### 二、商家报价（1.5h）

`PUT /api/customizations/{id}/quote`：添加多个报价明细（spec, quantity, unitPrice），计算总价，状态`quoted`。

#### 三、顾客确认（2h）

`PUT /api/customizations/{id}/confirm`：选择支付方式 → 自动生成临时商品（`is_customized=1`, `status='off'`）→ 调用下单逻辑生成订单 → 定制关联订单ID，状态`confirmed`。

#### 四、转为常规商品（1h）

`POST /api/customizations/{id}/convert-to-product`：商家设置分类、库存等信息，将临时商品转为正常商品。

#### 五、测试（0.5h）

验证定制全流程。

---

## 第8天：采购模块（商家创建采购单、厂家接单发货、入库）

**目标**：采购单创建、修改、厂家操作状态、确认收货入库。

**前置条件**：purchase_zj等表就绪。

### 任务详细 (6h)

#### 一、商家创建与修改采购单（2h）

- 创建：指定厂家、明细（product_id可为空）
- 修改：仅`pending`状态可改，可补全product_id，重新计算总额

#### 二、厂家移动端接口（2h）

厂家仅看到自己的采购单；操作：`pending`→`confirmed`（接单），`confirmed`→`shipped`（发货，填写物流）。

#### 三、商家确认收货与入库（1.5h）

`shipped`→`received`：遍历明细，已关联product_id的项直接增加库存并记录`purchase_in`流水；未关联的项报错阻止。

```java
for (PurchaseItem item : purchaseItems) {
    if (item.getProduct_id_zj() == null) throw new BusinessException("明细未关联商品，无法入库");
    productMapper.increaseStock(item.getProduct_id_zj(), item.getQuantity_zj());
    stockLogService.saveLog(item.getProduct_id_zj(), item.getQuantity_zj(), "purchase_in", ...);
}
```

#### 四、测试（0.5h）

验证采购全流程，含修改补全信息、入库拒绝场景。

---

## 第9天：赊账与还款（应收收款管理）

**目标**：实现赊账应收查询、顾客主动还款、商家登记线下收款。

**前置条件**：receivable_zj、receipt_zj表已存在。

### 任务详细 (6h)

#### 一、应收款列表与详情（1.5h）

`GET /api/receivables`（顾客看自己，商家看全部）、`GET /api/receivables/{id}`（含收款记录）。

#### 二、还款接口（2.5h）

`POST /api/receivables/{id}/receipts`：
- 顾客还款：仅`balance`方式，扣减余额，记录`repay`流水
- 商家登记线下收款：`cash`或`bank_transfer`，不涉及余额变动
- 更新应收的`amount_paid`和状态

```java
if ("balance".equals(paymentMethod) && currentUserRole.equals("customer")) {
    userService.decreaseBalance(userId, amount);
    balanceLogService.log(userId, -amount, "repay", ...);
}
receivable.setAmount_paid_zj(receivable.getAmount_paid_zj().add(amount));
// 更新状态
```

#### 三、测试（1h）

模拟赊账下单后还款，验证余额变动、应收状态更新。

---

## 第10天：库存管理完善、数据统计与系统配置

**目标**：完成库存日志查询、预警，增加管理后台统计数据接口，实现系统配置管理。

### 任务详细 (6h)

#### 一、库存日志分页查询（1h）

`GET /api/stock/logs`：支持按商品、类型筛选，分页。

#### 二、数据统计接口（2h）

`GET /api/admin/statistics/dashboard`：
- 今日/本周销售额、订单量
- 近7天销售趋势（按日聚合）
- 热销商品Top5
- 库存预警商品数量

```sql
SELECT DATE(created_at_zj) as date, SUM(actual_amount_zj) as total
FROM order_zj WHERE status_zj = 'completed' AND created_at_zj >= ...
GROUP BY DATE(created_at_zj)
```

#### 三、系统配置管理（1.5h）

`GET /api/configs` 获取全部配置；`PUT /api/configs/{key}` 修改配置项。

```java
@RestController
@RequestMapping("/api/configs")
public class ConfigController {
    @GetMapping
    public Result<Map<String, String>> list() { ... }
    @PutMapping("/{key}")
    public Result<?> update(@PathVariable String key, @RequestBody Map<String, String> body) { ... }
}
```

#### 四、库存预警与手动调整（1h）

- `GET /api/stock/warnings`：低于预警阈值的商品列表。
- `POST /api/stock/adjust`：手动增减库存，记录`manual_in/out`。

#### 五、测试（0.5h）

验证仪表盘数据、配置实时生效、预警列表。

---

## 第11天：Vue管理后台项目搭建、登录、仪表盘

**目标**：初始化Vue3 + Element Plus管理后台，实现登录鉴权、路由守卫、仪表盘数据展示。

**前置条件**：后端API已完成。

### 任务详细 (6h)

#### 一、项目初始化（1h）

使用Vite创建Vue3项目，安装vue-router、pinia、axios、element-plus、echarts。

#### 二、工具封装（1.5h）

- **axios封装**：baseURL、请求拦截器添加JWT（从localStorage获取），响应拦截处理401。
- **Pinia store**：存储token和用户信息，登录/退出方法。
- **路由配置**：各业务页面路由，路由守卫检查token。
- **动态菜单**：根据角色显示菜单。

#### 三、登录页面（1h）

手机号+密码表单，调用`/api/auth/login`，保存token，跳转仪表盘。

```javascript
const login = async () => {
  const res = await api.post('/auth/login', { phone, password })
  localStorage.setItem('token', res.data.token)
  userStore.setUserInfo(res.data)
  router.push('/dashboard')
}
```

#### 四、仪表盘页面（2h）

对接统计接口，使用ECharts绘制折线图、柱状图，展示卡片数据。

#### 五、基础布局验证（0.5h）

测试登录、退出、菜单导航、仪表盘数据展示。

---

## 第12天：管理后台商品与分类管理页面

**目标**：完成后台商品列表、新增/编辑商品、分类树管理页面。

### 任务详细 (6h)

#### 一、商品列表页（2h）

Element Plus的Table，分页、搜索、分类筛选，状态切换，操作按钮（编辑、下架/删除）。

#### 二、商品新增/编辑页（2.5h）

表单：名称、分类树选择、描述、图片上传（支持多图预览）、价格、库存、预警值、关联厂家。

#### 三、分类管理页（1.5h）

树形组件展示分类，添加节点、修改名称、删除（校验）。

#### 四、功能验证（0.5h）

测试新增商品、图片上传、分类增删改。

---

## 第13天：管理后台订单、定制、采购、库存、用户管理页面

**目标**：实现后台全部业务页面。

### 任务详细 (6h)

#### 一、订单管理（2h）

- 列表页：状态Tab（全部/已支付/已发货/已完成/已取消），搜索，操作。
- 详情页：商品明细、地址、物流、状态时间线，操作按钮（接单、发货（弹窗填物流）、取消）。

#### 二、定制管理（1h）

列表，详情：报价表单（多项规格），确认接单、转常规商品操作。

#### 三、采购管理（1h）

采购单列表，新增/编辑（选择厂家、添加明细），操作收货。

#### 四、库存管理（1h）

库存日志分页查询、预警列表、手动出入库表单。

#### 五、用户管理（1h）

用户列表，详情，调整余额/额度（弹窗输入正负值）。

#### 六、测试

各个页面功能测试。

---

## 第14天：Android客户端搭建与顾客端核心功能

**目标**：创建Android原生项目，实现顾客端登录、商品浏览、下单、订单管理。

**前置条件**：后端接口可用，Android Studio已安装。

### 任务详细 (6h)

#### 一、Android项目初始化（1.5h）

- 新建项目，添加依赖：Retrofit2、OkHttp、Gson、Glide。
- 封装ApiService接口（统一BaseURL），拦截器添加`Authorization`头。
- SharedPreferences存储Token和userId。
- 底部导航（BottomNavigationView + Fragment）：首页、定制、订单、我的。

#### 二、登录与个人中心（1h）

- LoginActivity：手机号密码登录，保存token，跳转MainActivity。
- MineFragment：展示余额、赊账额度；提供充值（弹窗）、修改密码（旧密码+新密码）、地址管理（EditText表单）。

#### 三、商品浏览与搜索（1h）

- 首页：RecyclerView展示分类，点击进入商品列表（SwipeRefreshLayout分页）。
- 商品详情页：ViewPager轮播图，价格库存，加入购物车或立即购买按钮。

#### 四、下单流程（1.5h）

- 确认订单Activity：展示商品清单，选择支付方式（RadioButton），显示默认地址（可修改）。
- 提交订单调用API，成功后跳转订单详情。

#### 五、订单列表与操作（1h）

- OrderFragment：TabLayout+ViewPager状态切换。
- 订单详情Activity：物流信息；“确认收货”按钮（shipped状态显示），“取消订单”（未发货状态显示）。

---

## 第15天：Android客户端商家/厂家功能与全面联调

**目标**：完成商家端、厂家端功能，全链路测试，准备答辩。

**前置条件**：基础框架完成，角色判断逻辑已实现。

### 任务详细 (6h)

#### 一、商家移动端（1.5h）

- 根据角色进入不同导航。
- 商品Fragment：查看全部商品（含下架），搜索。
- 订单Fragment：全部订单，提供“接单”、“发货”（填写物流）按钮。

#### 二、厂家移动端（1.5h）

- 厂家版导航：采购单、我的。
- PurchaseFragment：仅显示自己的采购单，列表点击详情。
- 详情Activity：操作“确认接单”、“发货”（填写物流）。

#### 三、顾客端定制功能（1h）

- 定制提交：填写描述，上传图片，调用API。
- 定制列表：查看状态，确认报价（选择支付方式，自动生成订单）。

#### 四、全链路联调（1.5h）

核心业务闭环：
- 顾客注册→充值→下单（余额/赊账）→商家接单发货→顾客确认收货→赊账单还款。
- 定制：提交→报价→确认生成订单→发货→完成。
- 采购：商家创建→厂家接单发货→商家收货入库。
异常场景：库存不足、余额不足、赊账超额、未发货取消。

#### 五、Bug修复与答辩准备（0.5h）

修复关键问题，准备演示脚本，录制视频，整理答辩材料。

---
